<!-- Views/Dialogs/AboutDialog.xaml -->
<Window x:Class="RssReader.Views.Dialogs.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RssReader.Views.Dialogs"
        mc:Ignorable="d"
        Title="About RSS Reader" 
        Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundColor}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- App Icon and Name -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
            <Image Width="48" Height="48" Source="/Resources/rss_icon.png" Margin="0,0,10,0"/>
            <TextBlock Text="RSS Reader" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center"
                       Foreground="{DynamicResource PrimaryTextColor}"/>
        </StackPanel>
        
        <!-- Version -->
        <TextBlock Grid.Row="1" Text="Version 1.0.0" HorizontalAlignment="Center" Margin="0,0,0,15"
                   Foreground="{DynamicResource SecondaryTextColor}"/>
        
        <!-- Description -->
        <TextBlock Grid.Row="2" TextWrapping="Wrap" Margin="0,0,0,15"
                   Foreground="{DynamicResource PrimaryTextColor}"
                   TextAlignment="Center" VerticalAlignment="Center">
            RSS Reader is a modern Windows application for subscribing to 
            and reading RSS feeds. Stay updated with the latest content from
            your favorite websites in one convenient place.
            
            <LineBreak/>
            <LineBreak/>
            
            © 2025 RSS Reader
        </TextBlock>
        
        <!-- Button -->
        <Button Grid.Row="3" Content="OK" Width="80" Height="30" 
                HorizontalAlignment="Center" IsDefault="True"
                Click="OkButton_Click"/>
    </Grid>
</Window>